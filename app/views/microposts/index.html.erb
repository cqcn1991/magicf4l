<div class="row">
    <div class="microposts-title span12">
      <h4 class="inline">正在流行……</h4>
    </div>

    <div class="new_microposts popular_microposts span12">
      <div class="row">
        <%= render partial: "new_microposts", collection: @popular_microposts, as: :micropost %>
      </div>
    </div>

   <div class="microposts-title span12">
      <h4 class="inline">最近分享……</h4>
     <%= link_to 'More', index2_microposts_path%>
    </div>

  <div class="new_microposts span12">
    <div class="row">
      <%= render partial: "new_microposts", collection: @new_microposts, as: :micropost  %>
    </div>
  </div>

</div>
<!--
<div class="list">
      <% @microposts.each do |micropost| %>
          <div>
              <%= link_to micropost.title, micropost.video_url.presence || micropost.article_url%>
            <div class="pull-right">
              <%= link_to 'Destroy', micropost, method: :delete, data: { confirm: 'Are you sure?' }, class: "pull-right" %>
              <i class="icon-heart"></i>
         <span><%= micropost.reputation_for(:likes).to_i %></span>
            </div>

          </div>
      <% end %>
    </div>

<% if current_user %>
<%= link_to 'New Micropost', new_micropost_path %>
<% end %>-->
